# Лабораторная работа 3.2
## Обнаружение отказов в распределённых системах. Протокол Gossip (Serf)

---

## Цель работы

Изучить принципы обнаружения отказов в распределённых системах на примере симуляции протокола Gossip (модель Serf), провести сравнительный анализ с другими подходами (Heartbeat, Ping) и исследовать влияние сетевых параметров на скорость конвергенции и нагрузку на сеть.

---

## Теоретическая часть

### Что такое обнаружение отказов?

Failure Detection — механизм, позволяющий узлам распределённой системы определять, когда другие узлы перестали функционировать. 

### Протокол Gossip

Gossip Protocol — децентрализованный вероятностный протокол, работающий по принципу распространения слухов

### Serf

Serf — реализация gossip-протокола от HashiCorp, используемая для:
- обнаружения узлов (membership)
- выявления сбоев (failure detection)
- оркестрации событий (event propagation)

### Ключевые параметры

| Параметр | Описание | Влияние |
|----------|----------|---------|
| **Gossip Interval (T)** | Время между раундами обмена (сек) | Меньше T → быстрее обнаружение, выше трафик |
| **Gossip Fanout (k)** | Количество опрашиваемых соседей | Больше k → быстрее сходимость, выше нагрузка |
| **Nodes (N)** | Общее количество узлов | Влияет на масштаб системы |
| **Packet Loss** | Процент потерь пакетов | Снижает вероятность доставки «слуха» |

### Понятие конвергенции

**Конвергенция** — состояние, при котором все живые узлы осведомлены о произошедшем отказе.

### Формула расчёта полосы пропускания

$$Bandwidth = \frac{1}{Interval} \times Fanout \times ActiveNodes \times PacketSize \times Overhead \times 8$$

где:
- $ActiveNodes = N \times (1 - FailurePercent)$
- $PacketSize = 1024$ байт
- $Overhead = 1.2$ (20% накладные расходы)

---

## Параметры варианта 7

| Параметр | Значение |
|----------|----------|
| **Gossip Interval** | 0.1, 0.2, 0.5 с |
| **Gossip Fanout** | 3, 5, 7 |
| **Nodes (N)** | 200 |
| **Packet Loss** | 1% | 
| **Node Failures** | 2% | 

---

## Этап 1. Расчёт полосы пропускания

Реализована функция `calc_bandwidth()`, вычисляющая теоретическое потребление трафика в битах в секунду по формуле из методики.

### Результаты расчёта 

**При фиксированном Fanout = 3:**

| Gossip Interval (с) | Bandwidth (Мбит/с) |
|---------------------|-------------------|
| 0.1 | 1.56 |
| 0.2 | 0.78 |
| 0.5 | 0.31 |

**При фиксированном Interval = 0.2 с:**

| Gossip Fanout | Bandwidth (Мбит/с) |
|---------------|-------------------|
| 3 | 0.78 |
| 5 | 1.30 |
| 7 | 1.82 |

**Анализ:**  
Bandwidth обратно пропорционален Gossip Interval (уменьшение интервала в 5 раз увеличивает трафик в 5 раз). Bandwidth линейно растёт с увеличением Fanout.

---

## Этап 2. Симуляция протоколов

Реализованы три класса-симулятора, наследуемых от единой логики:

### 1. SerfSimulator (Gossip)
- Узел выбирает `fanout` случайных соседей
- Учитывается вероятность потери пакета (`packet_loss`)
- Информация распространяется «вирусно»

### 2. HeartbeatSimulator (Full-mesh)
- Каждый узел пингует всех остальных соседей каждый такт
- Самая быстрая сходимость, но квадратичный рост трафика O(N²)

### 3. PingSimulator (Random Probe)
- Узел пингует одного случайного соседа
- Низкий трафик, но медленное обнаружение отказов

---

## Этап 3. Сравнительный анализ

Для сравнения протоколов запущена симуляция с параметрами:
- N = 200 узлов
- Отказавшие узлы = 2% (4 узла)
- Потери пакетов = 1%

---

## Результаты

Все эксперименты проведены с 5 прогонами для каждого набора параметров.

### Таблица 1. Влияние Gossip Interval (Fanout = 3)

| Interval (с) | Первое обнаружение (с) | Полная конвергенция (с) | Сообщений (шт) | Bandwidth (Мбит/с) |
|--------------|------------------------|------------------------|---------------|-------------------|
| 0.1 | 0.08 ± 0.02 | 0.52 ± 0.08 | 2450 ± 150 | 1.56 |
| 0.2 | 0.15 ± 0.03 | 0.95 ± 0.10 | 1850 ± 120 | 0.78 |
| 0.5 | 0.40 ± 0.05 | 2.85 ± 0.20 | 1250 ± 100 | 0.31 |

### Таблица 2. Влияние Gossip Fanout (Interval = 0.2 с)

| Fanout | Первое обнаружение (с) | Полная конвергенция (с) | Сообщений (шт) | Bandwidth (Мбит/с) |
|--------|------------------------|------------------------|---------------|-------------------|
| 3 | 0.15 ± 0.03 | 0.95 ± 0.10 | 1850 ± 120 | 0.78 |
| 5 | 0.11 ± 0.02 | 0.68 ± 0.08 | 2850 ± 180 | 1.30 |
| 7 | 0.09 ± 0.02 | 0.54 ± 0.07 | 3650 ± 220 | 1.82 |

---

## Рисунок 1. Влияние Gossip Interval

**Параметры:** N = 200, Fanout = 3, Packet Loss = 1%, Failures = 2%

<img width="1722" height="651" alt="image" src="https://github.com/user-attachments/assets/f3e0adaf-7e1a-4418-a28f-40ebd031ae2d" />

**а) Время первого обнаружения сбоя**  
При уменьшении интервала с 0.5 с до 0.1 с время первого обнаружения сокращается с ~0.40 с до ~0.08 с (в 5 раз). Зависимость близка к линейной.

**б) Время полной конвергенции**  
При Interval = 0.1 с конвергенция достигается за ~0.52 с. При Interval = 0.5 с — за ~2.85 с. Уменьшение интервала ускоряет распространение слуха пропорционально.

**в) Теоретическая полоса пропускания**  
Bandwidth обратно пропорционален интервалу: при 0.1 с → 1.56 Мбит/с, при 0.5 с → 0.31 Мбит/с.

**Вывод по рисунку 1:**  
Для минимизации времени конвергенции в крупной сети целесообразно использовать Gossip Interval = 0.1 с. Плата за это — рост полосы пропускания до ~1.6 Мбит/с.

---

## Рисунок 2. Влияние Gossip Fanout

**Параметры:** N = 200, Interval = 0.2 с, Packet Loss = 1%, Failures = 2%

<img width="1728" height="574" alt="image" src="https://github.com/user-attachments/assets/fb637532-a95b-4d48-9bab-b6d7a7530ac5" />

**а) Время первого обнаружения сбоя**  
Увеличение Fanout с 3 до 7 сокращает время первого обнаружения с ~0.15 с до ~0.09 с (ускорение на 40%).

**б) Время полной конвергенции**  
При Fanout = 3 конвергенция = 0.95 с. При Fanout = 7 конвергенция = 0.54 с. Ускорение ~43%.

**в) Сетевой трафик**  
Количество сообщений растёт линейно: с 1850 (Fanout=3) до 3650 (Fanout=7). Рост ~97%.

**Вывод по рисунку 2:**  
Fanout = 5 даёт оптимальный баланс между скоростью конвергенции (0.68 с) и нагрузкой на сеть (2850 сообщений). Fanout = 7 даёт дополнительное ускорение, но ценой значительного роста трафика. Fanout = 3 слишком медленный для крупной сети.

---

### Таблица 3. Сравнение протоколов (N=200, 2% отказов)

| Протокол | Время конвергенции (с) | Количество сообщений | Сложность трафика |
|----------|------------------------|---------------------|-------------------|
| **Serf (Gossip)** | 0.54 – 0.95 | ~2500 | O(N × Fanout) |
| **Heartbeat** | < 0.2 | ~40000 за раунд | O(N²) |
| **Ping** | > 30 | ~400 | O(N) |

### Анализ:

- **Heartbeat** — самое быстрое обнаружение (<0.2 с), но квадратичный рост трафика (40 000 сообщений за раунд). При масштабировании до 1000 узлов сеть ляжет.
- **Ping** — экономичный по трафику, но неприемлемо медленный (десятки секунд конвергенции).
- **Serf (Gossip)** — оптимальный компромисс: конвергенция <1 секунды при трафике ~2500 сообщений. Масштабируется логарифмически по времени и линейно по трафику.

---

## Выводы

1. **Влияние Gossip Interval:**  
   Уменьшение интервала с 0.5 с до 0.1 с сокращает время полной конвергенции с ~2.85 с до ~0.52 с (ускорение в 5.5 раз). Полоса пропускания при этом растёт обратно пропорционально (с 0.31 до 1.56 Мбит/с).

2. **Влияние Gossip Fanout:**  
   Увеличение Fanout с 3 до 7 ускоряет конвергенцию на 43% (с 0.95 с до 0.54 с), но увеличивает количество сообщений на 97% (с 1850 до 3650). Fanout = 5 даёт наилучший баланс: конвергенция 0.68 с при 2850 сообщениях.

3. **Оптимальная конфигурация для крупной сети (N=200):**
   - **Gossip Interval = 0.1 секунды**
   - **Gossip Fanout = 5**
   - Время полной конвергенции: ≈ 0.5 – 0.6 секунды
   - Полоса пропускания: ≈ 1.6 – 2.1 Мбит/с

4. **Сравнение с альтернативами:**  
   Serf (Gossip) обеспечивает наилучший компромисс между скоростью обнаружения и нагрузкой на сеть. Heartbeat быстрее, но неприемлем для крупных сетей из-за O(N²) трафика. Ping экономичен, но слишком медлителен.


